const UserModel = require('./User');
const PostModel = require('./Post');
const CommentModel = require('./Comment');
const ClapModel = require('./Clap');
const FollowModel = require('./Follow');

const models = {
  UserModel,
  PostModel,
  CommentModel,
  ClapModel,
  FollowModel,
};

module.exports = models;
